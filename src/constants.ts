export type Screen = {
  LOBBY: number;
  PLAYING: number;
  GAME_OVER: number;
  WIDTH: number;
  HEIGHT: number;
}

export const SCREEN: Screen = {
  LOBBY: 0,
  PLAYING: 1,
  GAME_OVER: 2,
  WIDTH: 400,
  HEIGHT: 300,
};
